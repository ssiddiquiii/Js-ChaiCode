"use strict" // treat all JS code as newer version

// alert("hello") // we are nodejs not browser

let name = "raju"
let age = 29
let isLoggedIn = true
let state;

// number => 2 to power 53
// bigint
// string => "",'',``
// boolean => true/fasle
// null => stand-alone value (object)
// undefined => 
// symbol => unique value

// object

console.log(typeof null) // object
console.log(typeof undefined) // undefined